# PocketWiki ships no reflection-based code, so the default Android and Compose
# rules are enough. This file exists because the release build type references
# it; keep it empty rather than deleting it, so enabling minification stays a
# one-line change.
